# Rust / Cargo
/src-tauri/target/
/desktop/target/
/desktop/gen/
**/*.rs.bk
Cargo.lock.orig

# Node / frontend
node_modules/
dist/
.vite/

# Local env / secrets (never commit)
.env
.env.*
# ...except the template, which holds placeholders only.
!.env.example
*.local

# Icon-generation source (regenerate with `npx tauri icon`); committed assets live in desktop/icons/
/app-icon.png

# OS
.DS_Store
Thumbs.db

# 로컬 데모 볼트 (headless example 산출물)
.knowsme-demo/

# 오너 개인 노트 (커밋 금지) — 나만 참조하는 자료(space-a 등)
/jb-docs/
